---
layout: default
title: "PersonalFinance App v1.0.0"
nav_exclude: true
---

# PersonalFinance App v1.0.0 — Release Notes

**Released:** 2026-05-20

---

## What's New

### Added
- Welcome email template for new users
- Automated checksum regeneration via pre-commit hook, reducing manual sync errors
- Hook now syncs release notes fingerprint after checksum updates, ensuring consistency

### Changed
- Reordered sidebar sections and added visual hierarchy styling for improved navigation
- Improved overall UI

### Fixed
- Stable fingerprint generation for Trust Audit, resolving a circular dependency in release notes
- Buyer-facing release notes accuracy and build distribution checksum sync
- Release pipeline republish handling

---

## How to Upgrade

**Use `upgrade.py` — never extract the zip manually.**
It preserves your `.env` (database credentials, license key, API key) and runs
any new database migrations automatically. Your portfolio data is untouched.

1. Download `pages_app_v1.0.0.zip` anywhere (e.g. your Downloads folder)
2. Open a terminal:
   - **Windows:** press `Win + R`, type `cmd`, press Enter
   - **Mac:** press `Cmd + Space`, type `Terminal`, press Enter
3. Run the upgrade command (replace the paths with your actual locations):

   **Windows:**
   ```
   python C:\pages_app\upgrade.py C:\Users\You\Downloads\pages_app_v1.0.0.zip
   ```
   **Mac:**
   ```
   python ~/pages_app/upgrade.py ~/Downloads/pages_app_v1.0.0.zip
   ```
4. Restart the app when it prints **"Upgrade complete."**

> **Tip:** Keep a copy of your `.env` file somewhere safe as a general backup.
> If you ever need to reinstall from scratch, it has everything you need.

> **First install?** Follow `GETTING_STARTED.md` from the top.

---

## Verify Your Download

Open the app and go to **Admin Tools → Trust Audit** — it verifies all files
automatically and shows a pass/fail result.

Expected fingerprint for `checksums.sha256`:
```
f54732cb858dd221d074ee99223e4322910dd5fa713f0c725f4bd6c031cf14b6
```

If Trust Audit reports a mismatch, do not use this copy — re-download from the official link.
