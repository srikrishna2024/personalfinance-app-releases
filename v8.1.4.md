---
layout: default
title: "Northstar v8.1.4"
nav_exclude: true
---

# Northstar v8.1.4 — Release Notes

**Released:** 2026-06-22

---

## What's New

There is not enough information in the commit messages to write meaningful release notes.

The only commit provided is `be31bd3 updated`, which does not describe any specific buyer-facing change. Writing release notes from this message would require inventing or inferring details not present in the source material, which would violate the rules above.

**To proceed, please provide commit messages that describe what actually changed.** For example:

- `fix: incorrect balance showing on dashboard after transfer`
- `add: export transactions to CSV`
- `change: updated date picker on budget screen`

---

## How to Upgrade

**Use `upgrade.py` — never extract the zip manually.**
It preserves your `.env` (database credentials, license key, API key) and runs
any new database migrations automatically. Your portfolio data is untouched.

1. Download your platform zip anywhere (e.g. your Downloads folder):
   - **Windows:** `pages_app_v8.1.4_windows.zip`
   - **Mac:** `pages_app_v8.1.4_mac.zip`
2. Open a terminal:
   - **Windows:** press `Win + R`, type `cmd`, press Enter
   - **Mac:** press `Cmd + Space`, type `Terminal`, press Enter
3. Run the upgrade command (replace the paths with your actual locations):

   **Windows:**
   ```
   python C:\pages_app\upgrade.py C:\Users\You\Downloads\pages_app_v8.1.4_windows.zip
   ```
   **Mac:**
   ```
   python ~/pages_app/upgrade.py ~/Downloads/pages_app_v8.1.4_mac.zip
   ```
4. Restart the app when it prints **"Upgrade complete."**

> **Tip:** Keep a copy of your `.env` file somewhere safe as a general backup.
> If you ever need to reinstall from scratch, it has everything you need.

> **First install?** Follow `GETTING_STARTED.md` from the top.

---

## Verify Your Download

Open the app and go to **Admin Tools → Trust Audit** — it verifies all files
automatically and shows a pass/fail result.

Expected fingerprint for `checksums.sha256` (must match your platform):

**Windows:**
```
7978dc8d995fc8d7d1009c9ffc0f5b5edc8b5020a04f975673cf8a53a9899bc0
```

**Mac:**
```
7978dc8d995fc8d7d1009c9ffc0f5b5edc8b5020a04f975673cf8a53a9899bc0
```

If Trust Audit reports a mismatch, do not use this copy — re-download from the official link.
