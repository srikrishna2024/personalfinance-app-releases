---
layout: default
title: "Northstar v8.1.5"
nav_exclude: true
---

# Northstar v8.1.5 — Release Notes

**Released:** 2026-06-22

---

## What's New

There is not enough information in the available commit messages to write meaningful release notes for version 8.1.5.

The only commit provided — `f01f9ed updated` — does not describe what was changed, added, fixed, or removed in any specific or usable way. Following the rules strictly, inventing or inferring content from a vague message like "updated" is not permitted.

**To proceed, please provide commit messages that describe actual changes**, for example:
- What file or feature was updated
- What problem was fixed, or what behavior changed
- Any user-facing context included in the commit body or PR description

---

## How to Upgrade

**Use `upgrade.py` — never extract the zip manually.**
It preserves your `.env` (database credentials, license key, API key) and runs
any new database migrations automatically. Your portfolio data is untouched.

1. Download your platform zip anywhere (e.g. your Downloads folder):
   - **Windows:** `pages_app_v8.1.5_windows.zip`
   - **Mac:** `pages_app_v8.1.5_mac.zip`
2. Open a terminal:
   - **Windows:** press `Win + R`, type `cmd`, press Enter
   - **Mac:** press `Cmd + Space`, type `Terminal`, press Enter
3. Run the upgrade command (replace the paths with your actual locations):

   **Windows:**
   ```
   python C:\pages_app\upgrade.py C:\Users\You\Downloads\pages_app_v8.1.5_windows.zip
   ```
   **Mac:**
   ```
   python ~/pages_app/upgrade.py ~/Downloads/pages_app_v8.1.5_mac.zip
   ```
4. Restart the app when it prints **"Upgrade complete."**

> **Tip:** Keep a copy of your `.env` file somewhere safe as a general backup.
> If you ever need to reinstall from scratch, it has everything you need.

> **First install?** Follow `GETTING_STARTED.md` from the top.

---

## Verify Your Download

Open the app and go to **Admin Tools → Trust Audit** — it verifies all files
automatically and shows a pass/fail result.

Expected fingerprint for `checksums.sha256` (must match your platform):

**Windows:**
```
67fbd1c993d73a9934722824afe7caed2058843b9e80105e9bf12f56de79b38e
```

**Mac:**
```
67fbd1c993d73a9934722824afe7caed2058843b9e80105e9bf12f56de79b38e
```

If Trust Audit reports a mismatch, do not use this copy — re-download from the official link.
